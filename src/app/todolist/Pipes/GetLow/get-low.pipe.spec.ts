/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GetLowPipe } from './get-low.pipe';

describe('GetLowPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLowPipe();
    expect(pipe).toBeTruthy();
  });
});
